name: Hola mundo!!!

on: [push]

jobs:
  hola:
    runs-on: ubuntu-latest
  
  steps:
    - name: "Hello, World!"
      run: echo "Hello, World!"


#  build:
#    steps:
#    - name: Checkout code
#      uses: actions/checkout@v2
#
#    - name: Build Docker image
#